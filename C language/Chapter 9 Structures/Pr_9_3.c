#include <stdio.h>
int main()
{
    // An array stores in contigious memory location and it is also
    // same for an array prefrence will depend upon the type of data
    printf("An array stores in contigious memory location and it is also \n same for an array prefrence will depend upon the type of data");
    return 0;
}