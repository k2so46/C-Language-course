#include <stdio.h>
int main()
{
    char *str = "This is a line";
    puts(str);
    printf("The program will run successfully with the puts() and so it helps us to read the multi word string");
    // The program will run successfully with the puts() and so it
    // helps us to read the multi word string
    return 0;
}